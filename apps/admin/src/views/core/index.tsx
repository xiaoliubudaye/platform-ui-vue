import { defineComponent } from 'vue';

export default defineComponent({
    name: 'Index',
    setup() {
        return () => (
            <div>
                <div class="text-center">Index</div>
            </div>
        );
    },
});
