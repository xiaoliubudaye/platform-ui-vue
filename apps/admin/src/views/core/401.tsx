import { defineComponent } from 'vue';

export default defineComponent({
    name: '401',
    setup() {
        return () => (
            <div>
                <div class="text-center">401</div>
            </div>
        );
    },
});
